[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "casp17-pl"
version = "0.1.0"
description = "Unified uv-based workspace for CASP17 protein-ligand skill and MCP development."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "gemmi>=0.7.5",
  "numpy>=2.4.4",
  "pyyaml>=6.0.2",
  "rdkit>=2026.3.1",
]

[project.optional-dependencies]
mcp = [
  "mcp>=1.0.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0.0",
  "ruff>=0.8.0",
]

[project.scripts]
casp17-pl = "casp17.cli:main"
casp17-pl-mcp = "casp17.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/casp17"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
package = true
default-groups = ["dev"]

[tool.ruff]
line-length = 100
target-version = "py312"
